import styled from 'styled-components';

const x = styled.div`
  height: 200px;
  color: ${isBlue => isBlue => isBlue ? 'blue' : 'red'};
  width:  100px;

  &:hover{
    bottom: 20px;
    left: 30px;
    top: 23px;
  }
`;